export default interface IUnitCondition {
  UNIT_NAME: {
    MAX: number;
    REQUIRED: boolean;
  };

  UNIT_DESCRIPTION: {
    MAX: number;
    REQUIRED: boolean;
  };

  UNIT_NO: {
    REQUIRED: boolean;
  };
}
