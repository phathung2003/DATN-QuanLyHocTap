export type Package = {
  id: string;
  title: string;
  category: string;
  status: string;
  view: number;
};

export type Invoice = {
  name: string;
  price: number;
  invoiceDate: string;
  status: string;
};
